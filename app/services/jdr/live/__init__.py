"""Live-mode sub-router for the JDR service.

At Jalon 5 this package only ships the *contract* (POST + WS endpoints)
so the public API surface is frozen before Discord ingestion lands at
Jalon 6+. See :mod:`app.services.jdr.live.router`.
"""
